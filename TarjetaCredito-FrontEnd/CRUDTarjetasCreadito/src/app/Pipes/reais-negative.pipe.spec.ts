import { ReaisNegativePipe } from './reais-negative.pipe';

describe('ReaisNegativePipe', () => {
  it('create an instance', () => {
    const pipe = new ReaisNegativePipe();
    expect(pipe).toBeTruthy();
  });
});
